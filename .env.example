# Copy to .env for local testing, or set these in Vercel → Settings → Environment Variables.
# Leave blank to run on mock data.

VITE_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
VITE_API_SECRET=your-long-random-secret-here
